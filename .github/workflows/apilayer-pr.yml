name: ApiLayer

on:
    pull_request:
        branches: [ main ]
        paths:
            - 'apps/apilayer/**'
jobs:
    pr_checks:
        steps:
            uses: apps/apilayer/.github/workflows/pr.yml@main