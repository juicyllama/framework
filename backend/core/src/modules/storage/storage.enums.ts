export enum StorageFileType {
	PUBLIC = 'PUBLIC',
	PRIVATE = 'PRIVATE',
}

export enum StorageFileFormat {
	BLOB = 'BLOB',
	JSON = 'JSON',
	CSV = 'CSV',
	Express_Multer_File = 'Express_Multer_File',
}
