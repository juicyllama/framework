export enum WordpresMediaStatus {
	'publish' = 'publish',
	'future' = 'future',
	'draft' = 'draft',
	'pending' = 'pending',
	'private' = 'private',
	'inherit' = 'inherit',
	'trash' = 'trash',
}

export enum WordpressMediaType {
	'image' = 'image',
	'file' = 'file',
}
